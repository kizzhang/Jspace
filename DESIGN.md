# 产品设计说明

## 核心原则

1. **原始资料不搬家**：对话 JSONL、实验产物和 PDF 仍是事实来源，工作台只维护可重建索引。
2. **自动记录，人类定论**：自动摘要和状态推断负责降低整理成本；“我的补充”永远单独保存，不被下一次整理覆盖。
3. **结论必须可回溯**：对话保留逐条问答，实验保留最近文件清单，文献保留 DOI / arXiv / PDF 指纹和原 PDF。
4. **合并而非静默丢弃**：文献用稳定身份去重；再次收录同一 DOI、arXiv ID、PDF 指纹或规范化标题时更新同一记录。
5. **本地优先**：默认只监听回环地址；除公开文献元数据更新外，不上传研究资料。

## 当前信息模型

```text
每日整理
├── AI 对话（Codex / Claude Code）
│   ├── 会话标题、时间、全文
│   └── 人类研究备注
├── 实验（EXP 编号）
│   ├── 脚本 / 启动器
│   ├── 结果 / manifest
│   ├── 预注册 / settlement
│   └── 结论、异常、下一步
└── 文献（canonical identity）
    ├── DOI / arXiv / 标题
    ├── PDF SHA-256
    ├── 元数据更新时间
    ├── 阅读状态与简短备注
    └── PDF 内嵌预览
```

## 第一版状态推断

- 找到 settlement / closure / conclusion：`已结论`
- 有结果文件：`有结果`
- 有启动脚本：`已配置`
- 有 preregistration：`已预注册`
- 其余：`草稿`

这是辅助导航，不替代研究者的最终判断。

## 后续优先级建议

1. **Zotero 双向同步**：如果已经使用 Zotero，让 Zotero 继续负责引用、批注和移动端同步；工作台只吸收条目、阅读状态、批注回链。
2. **实验运行采集器**：为训练入口增加薄包装，自动记录命令、Git commit、环境、参数、指标和产物哈希。
3. **结论—证据图谱**：把一个结论链接到支持/反驳它的实验、对话片段和文献段落，并标记置信度。
4. **文献更新监控**：按 DOI / arXiv 检查版本、勘误、撤稿和引用增量，变化进入每日整理而不是直接覆盖备注。
5. **备份与导出**：生成 Markdown 日报和机器可读 JSON 快照，便于 Git/LFS 或加密备份。
